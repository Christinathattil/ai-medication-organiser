#!/usr/bin/env node
// This script was used for SMS reminders via Twilio.
// SMS functionality has been removed from the application.
// Browser notifications are now handled directl in enhanced-server.js via node-cron

console.log('⚠️  SMS reminders have been disabled. Use browser notifications instead.');
process.exit(0);
